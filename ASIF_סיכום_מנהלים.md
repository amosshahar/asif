# ASIF — אפליקציית איסוף הזמנות לסופרמרקט
### סיכום מנהלים — דרישות המוצר
**גרסה:** 1.0 | **תאריך:** אפריל 2026

---

## מה האפליקציה עושה

ASIF היא אפליקציה לאייפון המיועדת לליקוטנים (קולקטורים) בסופרמרקט, האוספים הזמנות מקוונות מהמדפים. האפליקציה:

- מתחברת למערכת ההזמנות ומציגה לליקוטן את ההזמנה המוקצית לו
- מנחה אותו בנתיב האיסוף היעיל ביותר בתוך החנות
- מאפשרת סריקת ברקוד, שקילה מהמשקל ואישור ידני של כל פריט
- מאפשרת סימון פריט כחסר במידה ואינו זמין
- בסיום — שולחת את נתוני ההזמנה המושלמת למערכת ה-ERP
- מציגה למנהל דשבורד חי עם סטטוס כל הליקוטנים בזמן אמת

---

## משתמשים

| משתמש | תפקיד |
|---|---|
| ליקוטן | מתחבר מהאייפון, מקבל הזמנה, מבצע איסוף |
| מנהל משמרת | צופה בדשבורד — מי עובד על איזו הזמנה, כמה פריטים נאספו, שעת התחלה |
| מערכת ERP | מקבלת את נתוני ההזמנה הגמורה |

---

## זרימת עבודה

```
התחברות → קבלת הזמנה → ניווט לפי רשימת איסוף ממוינת →
סריקה / שקילה / אישור ידני לכל פריט → סימון פריטים חסרים →
סיום ושליחה ל-ERP
```

---

## פונקציות מרכזיות

### התחברות
- כניסה עם מזהה עובד + קוד PIN
- שמירת התחברות למשך המשמרת

### רשימת איסוף
- הפריטים ממוינים לפי הנתיב היעיל ביותר בחנות
- קיבוץ לפי מעבר / קטגוריה עם כותרת מעבר וסרגל התקדמות
- כרטיס פריט כולל: תמונה, שם מוצר, כמות, מיקום במדף, **הערת לקוח לפריט הספציפי** (תמיד גלויה, לא דורשת לחיצה), תג "שקול" לפריטים הנמדדים בשקל

### שיטות איסוף
1. **סריקת ברקוד** — סריקה עם מצלמת האייפון, אישור מיידי עם רטט + צליל
2. **שקילה** — חיבור למשקל (BLE / USB-C / WiFi, לפי הדגם שייבחר), משקל מתעדכן אוטומטית, חישוב מחיר אוטומטי לפי משקל × מחיר ליחידה; קיים גיבוי להזנה ידנית
3. **אישור ידני** — לפריטים ללא ברקוד; מסומן בסיכום ההזמנה

### פריט חסר
- ליקוטן בוחר סיבה: אזל מהמלאי / לא נמצא / פגום
- מסומן בהזמנה ונשלח ל-ERP
- תחליפים — גרסה 2

### דשבורד מנהל
- צפייה חיה: שם ליקוטן, מספר הזמנה, התקדמות (x מתוך y פריטים), שעת התחלה, סטטוס (פנוי / בתהליך / הושלם), מספר פריטים חסרים
- הקצאת הזמנה לליקוטן
- צפייה בפירוט הזמנה פעילה או מושלמת

### שליחה ל-ERP
- בסיום ההזמנה נשלח payload מלא: פרטי פריטים שנאספו, משקלים, פריטים חסרים, שיטת איסוף, חותמות זמן


---

## מפת דרכים

| שלב | תכולה |
|---|---|
| **MVP** | התחברות, הזמנה בודדת, רשימת איסוף, סריקה, שקילה, אישור ידני, פריט חסר, שליחה ל-ERP, דשבורד מנהל |
| **גרסה 2** | תחליפים, הצעות אוטומטיות, הודעות SMS ללקוח, אישור לקוח |

---

## שאלות פתוחות — נדרש מידע לפני תחילת פיתוח

### 1. מערכת ההזמנות
> **שם המערכת ממנה מגיעות ההזמנות?**
> פרטי התחברות: כתובת שרת / API endpoint, שיטת אימות (API key / OAuth / אחר), מבנה הנתונים (schema) של הזמנה ופריטים.

---

### 2. משקל — חיבור ודגם
> **איזה משקל ישמש את הליקוטנים?**
> יש לבחור בין שלוש אפשרויות:
> - **Bluetooth (BLE)** — נייד, ללא כבל, דורש הצמדה חד-פעמית
> - **USB-C / Lightning** — אמין יותר, אין צורך בהצמדה, הטלפון כבול לכבל
> - **WiFi** — מתאים לתחנת שקילה קבועה
>
> **נדרש: שם הדגם המדויק של המשקל** לצורך פיתוח ממשק התקשורת.

---

### 3. מערכת ERP — דיווח הזמנות גמורות
> **לאיזו מערכת ERP נשלח את נתוני ההזמנה המושלמת?**
> נדרש:
> - שם המערכת (SAP / Priority / NetSuite / מותאמת אישית / אחר)
> - קישור לתיעוד ה-API
> - שיטת אימות (API key / OAuth / webhook)
> - מבנה ה-payload הנדרש (אילו שדות, באיזה פורמט)

---

### 4. לוגיקת הנתיב היעיל
> **כיצד קובעים את סדר האיסוף האופטימלי בחנות?**
> אפשרויות נפוצות:
> - רשימת מעברים לפי סדר מספרי / גיאוגרפי ידוע
> - מיפוי מדפים — כל מוצר מוקצה מיקום (מעבר + מדף + עמדה)
> - כלל אחר שמוגדר בחנות
>
> **נדרש: תיאור הלוגיקה, או קובץ / טבלה של מיפוי מוצר → מיקום בחנות.**

---

