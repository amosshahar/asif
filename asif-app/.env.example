# Dev (`npm run dev`) — browser / tooling
# VITE_API_URL=http://localhost:3002

# Production bundle: physical device uses PRODUCTION; iOS Simulator uses SIMULATOR (see src/api.ts).
# VITE_API_URL_PRODUCTION=https://your-asif-api.example.com
# VITE_API_URL_SIMULATOR=http://127.0.0.1:3002
# VITE_FORCE_DEVICE_API=true
